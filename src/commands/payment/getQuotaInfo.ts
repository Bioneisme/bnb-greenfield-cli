
import { GreenfieldClient } from "../../utils/sdk";


// Create an object with the required properties

// export async function getQuotaInfo(creator:string, to:string, amount:string) {
//     try {
//         const bucket = await GreenfieldClient.client.bucket.getBucketReadQuota()
//         console.log(`deposit created`);
//     } catch (e) {
//         console.error(`payment account creation failed: ${e}`);
//     }
// }
//
